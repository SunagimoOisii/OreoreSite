{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Codex: workspace-write (on-failure, network-off)",
      "type": "shell",
      "command": "codex",
      "args": [
        "--approval", "on-failure",
        "--sandbox", "workspace-write",
        "--network", "disabled"
      ],
      "options": {
        "env": {
          "CODEX_APPROVAL_MODE": "on-failure",
          "CODEX_SANDBOX_MODE": "workspace-write",
          "CODEX_NETWORK": "disabled"
        }
      },
      "problemMatcher": []
    }
  ]
}
